name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    environment:
      name: pass_code

    steps:

      # ==========================================
      # 1. Descargar el repositorio
      # ==========================================
      - name: Checkout
        uses: actions/checkout@v4


      # ==========================================
      # 2. Generar / sobrescribir configuracion de entornos
      # ==========================================
      - name: Generar configuracion de entornos
        env:
          PIN: ${{ secrets.PASS_CODE }}
          APP_NAME: ${{ vars.APP_NAME }}
        run: |
          echo "========================================="
          echo "Generando configuración de entornos"
          echo "========================================="

          # Validar que el secreto exista
          if [ -z "$PIN" ]; then
            echo "ERROR: PASS_CODE no está configurado"
            exit 1
          fi

          # Validar que la variable exista
          if [ -z "$APP_NAME" ]; then
            echo "ERROR: APP_NAME no está configurado"
            exit 1
          fi

          # Asegurar que exista la carpeta js
          mkdir -p js

          # Mostrar estado anterior del archivo
          echo ""
          echo "Estado anterior del archivo:"

          if [ -f "js/environments.js" ]; then
            echo "El archivo existe."

            FILE_SIZE=$(wc -c < js/environments.js)

            echo "Tamaño anterior: ${FILE_SIZE} bytes"
          else
            echo "El archivo no existía."
          fi

          # ==========================================
          # Sobrescribir el archivo
          # ==========================================
          printf 'window.PIN_ACCESO = "%s";\nwindow.APP_NAME = "%s";\n' "$PIN" "$APP_NAME" > js/environments.js

          echo ""
          echo "Archivo sobrescrito."


      # ==========================================
      # 3. Validar que el archivo fue sobrescrito
      # ==========================================
      - name: Validar environments.js
        env:
          PIN: ${{ secrets.PASS_CODE }}
          APP_NAME: ${{ vars.APP_NAME }}
        run: |
          echo "========================================="
          echo "Validando environments.js"
          echo "========================================="

          # ------------------------------------------
          # Validar existencia
          # ------------------------------------------
          if [ ! -f "js/environments.js" ]; then
            echo "ERROR: js/environments.js no existe"
            exit 1
          fi

          echo "OK: El archivo existe."


          # ------------------------------------------
          # Validar que no esté vacío
          # ------------------------------------------
          if [ ! -s "js/environments.js" ]; then
            echo "ERROR: js/environments.js está vacío"
            exit 1
          fi

          echo "OK: El archivo no está vacío."


          # ------------------------------------------
          # Validar estructura
          # ------------------------------------------
          if ! grep -q '^window\.PIN_ACCESO = ' js/environments.js; then
            echo "ERROR: El archivo no contiene window.PIN_ACCESO"
            exit 1
          fi

          echo "OK: La variable window.PIN_ACCESO existe."

          if ! grep -q '^window\.APP_NAME = ' js/environments.js; then
            echo "ERROR: El archivo no contiene window.APP_NAME"
            exit 1
          fi

          echo "OK: La variable window.APP_NAME existe."


          # ------------------------------------------
          # Validar que el contenido corresponde
          # al secreto y variable configurados
          # ------------------------------------------
          if ! grep -Fq "$PIN" js/environments.js; then
            echo "ERROR: El contenido del archivo no coincide con PASS_CODE"
            exit 1
          fi

          echo "OK: El contenido coincide con PASS_CODE."

          if ! grep -Fq "$APP_NAME" js/environments.js; then
            echo "ERROR: El contenido del archivo no coincide con APP_NAME"
            exit 1
          fi

          echo "OK: El contenido coincide con APP_NAME."


          # ------------------------------------------
          # Mostrar solamente información segura
          # ------------------------------------------
          FILE_SIZE=$(wc -c < js/environments.js)

          echo ""
          echo "========================================="
          echo "VALIDACION EXITOSA"
          echo "========================================="
          echo "Archivo: js/environments.js"
          echo "Tamaño: ${FILE_SIZE} bytes"
          echo "Estado: SOBRESCRITO CORRECTAMENTE"
          echo "PIN: oculto"
          echo "APP_NAME: oculto"
          echo "========================================="


      # ==========================================
      # 4. Verificar estructura del proyecto
      # ==========================================
      - name: Verificar estructura del proyecto
        run: |
          echo "========================================="
          echo "Estructura del proyecto"
          echo "========================================="

          find . -maxdepth 3 -type f \
            ! -path './.git/*' \
            | sort

          echo ""
          echo "Verificando archivo de entornos..."

          test -f js/environments.js

          echo "OK: js/environments.js está presente."


      # ==========================================
      # 5. Configurar GitHub Pages
      # ==========================================
      - name: Configure Pages
        uses: actions/configure-pages@v5


      # ==========================================
      # 6. Crear artifact para GitHub Pages
      # ==========================================
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .


      # ==========================================
      # 7. Deploy a GitHub Pages
      # ==========================================
      - name: Deploy
        uses: actions/deploy-pages@v4